import 'package:flutter_test/flutter_test.dart';

void main() {
  // Widget tests are handled via integration tests.
  // Domain logic is covered in test/domain/.
  test('placeholder', () => expect(true, isTrue));
}
